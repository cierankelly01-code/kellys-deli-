# The client calls the API at same-origin "/api" and Vite proxies it in dev
# (see vite.config.ts). For a split deployment, set this to the API base URL
# and use it in src/lib/api.ts.
VITE_API_URL=""
